{
  "scripts": {
    "dev": "vite",
    "dev:all": "concurrently \"vercel dev --listen 3000\" \"vite\"",
    "dev:backend": "vercel dev --listen 3000",
    "dev:frontend": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  }
}
