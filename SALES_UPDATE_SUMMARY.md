# 🛒 อัปเดตระบบยอดขาย - เพิ่มฟีเจอร์แก้ไขและลบ

## 📋 สรุปการอัปเดต

เพิ่มฟีเจอร์ใหม่ในหน้ายอดขาย:
- ✅ **แก้ไขยอดขาย** - สามารถแก้ไขข้อมูลยอดขายที่บันทึกไว้แล้ว
- ✅ **ลบยอดขาย** - ลบยอดขายที่ไม่ต้องการออกจากระบบ
- ✅ **จำนวนที่ขาย** - เพิ่ม StatCard แสดงจำนวนชิ้นที่ขายวันนี้
- ✅ **Activity Logging** - บันทึกการแก้ไขและลบยอดขาย

---

## 🆕 ฟีเจอร์ใหม่

### 1. แก้ไขยอดขาย ✏️

**วิธีใช้:**
1. ไปที่หน้า **ยอดขาย** (`/sales`)
2. คลิกปุ่ม **⋮** (More) ที่แถวยอดขายที่ต้องการ
3. เลือก **แก้ไข**
4. แก้ไขข้อมูล:
   - เปลี่ยนเกม
   - เปลี่ยนรายการเติม
   - แก้ไขจำนวน
   - แก้ไขส่วนลด
5. ดูสรุปยอดใหม่
6. คลิก **บันทึกการแก้ไข**

**สิ่งที่ระบบจะทำ:**
- คำนวณต้นทุน ราคาขาย และกำไรใหม่อัตโนมัติ
- อัปเดตข้อมูลในฐานข้อมูล
- บันทึก Activity Log
- รีเฟรชข้อมูลหน้าจอ

### 2. ลบยอดขาย 🗑️

**วิธีใช้:**
1. ไปที่หน้า **ยอดขาย** (`/sales`)
2. คลิกปุ่ม **⋮** (More) ที่แถวยอดขายที่ต้องการ
3. เลือก **ลบ**
4. ยืนยันการลบ
5. ระบบจะลบยอดขายทันที

**⚠️ คำเตือน:**
- การลบจะไม่สามารถกู้คืนได้
- กรุณาตรวจสอบความถูกต้องก่อนลบ
- ระบบจะบันทึก Activity Log การลบ

### 3. สถิติจำนวนที่ขาย 📦

**Card ใหม่:**
- แสดงจำนวนชิ้นที่ขายวันนี้
- คำนวณจากผลรวม `quantity` ของยอดขายทั้งหมดวันนี้
- อัปเดตเรียลไทม์เมื่อเพิ่ม/แก้ไข/ลบยอดขาย

**ตัวอย่าง:**
```
┌────────────────────────┐
│ 📦 จำนวนที่ขาย          │
│    150                 │
│    ชิ้นที่ขายวันนี้      │
└────────────────────────┘
```

---

## 📁 ไฟล์ที่แก้ไข/เพิ่ม

### ✅ ไฟล์ใหม่:
```
src/components/EditSaleDialog.tsx    # Dialog แก้ไขยอดขาย
SALES_UPDATE_SUMMARY.md              # เอกสารสรุป (ไฟล์นี้)
```

### ✅ ไฟล์ที่แก้ไข:
```
src/lib/salesUtils.ts                # เพิ่มฟังก์ชัน updateSale()
src/pages/Sales.tsx                  # เพิ่ม UI และ logic
```

---

## 🔧 รายละเอียดทางเทคนิค

### 1. ฟังก์ชัน updateSale()

**Location:** `src/lib/salesUtils.ts`

```typescript
export async function updateSale(
  saleId: string,
  saleData: SaleInput,
  userInfo?: { userId: string; email: string; shopName?: string }
): Promise<{ success: boolean; error?: string }>
```

**การทำงาน:**
1. ดึงข้อมูลเกมและรายการเติมที่เลือก
2. คำนวณต้นทุน ราคาขาย ส่วนลด และกำไรใหม่
3. อัปเดตข้อมูลใน Firestore
4. บันทึก Activity Log
5. Return ผลลัพธ์

**ตัวอย่างการใช้:**
```typescript
const result = await updateSale(
  "sale123",
  {
    gameId: "game456",
    gameItemId: "item789",
    quantity: 5,
    discount: 10,
  },
  {
    userId: user.uid,
    email: user.email,
    shopName: userData.shopName,
  }
);

if (result.success) {
  console.log("✅ แก้ไขสำเร็จ");
} else {
  console.error("❌ Error:", result.error);
}
```

### 2. EditSaleDialog Component

**Location:** `src/components/EditSaleDialog.tsx`

**Props:**
```typescript
interface EditSaleDialogProps {
  open: boolean;                    // เปิด/ปิด dialog
  onOpenChange: (open: boolean) => void;  // Handle เปิด/ปิด
  sale: Sale | null;                // ยอดขายที่จะแก้ไข
  games: Game[];                    // รายการเกมทั้งหมด
  gameItems: GameItem[];            // รายการเติมทั้งหมด
  onSuccess: () => Promise<void>;   // Callback เมื่อสำเร็จ
}
```

**Features:**
- โหลดข้อมูลยอดขายเข้าฟอร์มอัตโนมัติ
- Select เกม และกรองรายการเติมตามเกม
- แสดงรายละเอียดรายการเติม (ราคา, ต้นทุน, กำไร)
- คำนวณยอดรวมเรียลไทม์
- Validation ข้อมูล
- Loading state

### 3. Sales Page Updates

**Location:** `src/pages/Sales.tsx`

**การเปลี่ยนแปลง:**

#### เพิ่ม State:
```typescript
const [editDialogOpen, setEditDialogOpen] = useState(false);
const [selectedSale, setSelectedSale] = useState<Sale | null>(null);
const [stats, setStats] = useState({
  todaySales: 0,
  todayProfit: 0,
  todayOrders: 0,
  todayQuantity: 0,  // ← ใหม่
});
```

#### เพิ่ม Functions:
```typescript
// แก้ไขยอดขาย
const handleEdit = (sale: Sale) => {
  setSelectedSale(sale);
  setEditDialogOpen(true);
};

// ลบยอดขาย (อัปเดตให้ส่ง userInfo)
const handleDelete = async (sale: Sale) => {
  // ...
  await deleteSale(sale.id, {
    userId: user.uid,
    email: user.email || "",
    shopName: userData.shopName,
    saleName: `${sale.gameName} - ${sale.gameItemName}`,
  });
};
```

#### เพิ่ม StatCard:
```tsx
<Card className="border-border bg-card shadow-card">
  <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
    <CardTitle className="text-sm font-medium">จำนวนที่ขาย</CardTitle>
    <Package className="h-4 w-4 text-muted-foreground" />
  </CardHeader>
  <CardContent>
    <div className="text-2xl font-bold">{stats.todayQuantity}</div>
    <p className="text-xs text-muted-foreground">ชิ้นที่ขายวันนี้</p>
  </CardContent>
</Card>
```

#### เปลี่ยนปุ่มเป็น Dropdown Menu:
```tsx
<DropdownMenu>
  <DropdownMenuTrigger asChild>
    <Button variant="ghost" size="sm">
      <MoreVertical className="h-4 w-4" />
    </Button>
  </DropdownMenuTrigger>
  <DropdownMenuContent align="end">
    <DropdownMenuItem onClick={() => handleEdit(sale)}>
      <Edit className="mr-2 h-4 w-4" />
      แก้ไข
    </DropdownMenuItem>
    <DropdownMenuSeparator />
    <DropdownMenuItem
      onClick={() => handleDelete(sale)}
      className="text-destructive"
    >
      <Trash2 className="mr-2 h-4 w-4" />
      ลบ
    </DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>
```

---

## 📊 Activity Logging

### การแก้ไขยอดขาย:
```json
{
  "action": "sale_updated",
  "details": "แก้ไขยอดขาย \"ROV - Diamond 100\" จำนวน 5 รายการ (฿495.00)",
  "metadata": {
    "saleId": "sale123",
    "gameId": "game456",
    "gameName": "ROV",
    "gameItemId": "item789",
    "gameItemName": "Diamond 100",
    "quantity": 5,
    "netAmount": 495,
    "profit": 45
  }
}
```

### การลบยอดขาย:
```json
{
  "action": "sale_deleted",
  "details": "ลบยอดขาย \"ROV - Diamond 100\" (ID: sale123)",
  "metadata": {
    "saleId": "sale123"
  }
}
```

---

## 🎨 UI/UX Improvements

### ก่อนอัปเดต:
```
┌─────────────────────────────────────┐
│ ยอดขาย           [+ เพิ่มยอดขาย]    │
├─────────────────────────────────────┤
│ [ยอดขายวันนี้] [กำไร] [รายการ]      │
├─────────────────────────────────────┤
│ วันที่  เกม  รายการ  ...  [🗑️]     │
│ 01/01  ROV   Diamond  ...  [🗑️]     │
└─────────────────────────────────────┘
```

### หลังอัปเดต:
```
┌──────────────────────────────────────────┐
│ ยอดขาย              [+ เพิ่มยอดขาย]      │
├──────────────────────────────────────────┤
│ [ยอดขาย] [กำไร] [รายการ] [จำนวนที่ขาย] │
├──────────────────────────────────────────┤
│ วันที่  เกม  รายการ  ...  [⋮]           │
│ 01/01  ROV   Diamond  ...  [⋮] ←Dropdown │
│                               ├─ ✏️ แก้ไข│
│                               └─ 🗑️ ลบ   │
└──────────────────────────────────────────┘
```

---

## 🧪 การทดสอบ

### Test Cases:

#### ✅ แก้ไขยอดขาย
```
1. เปิด EditSaleDialog
   → แสดงข้อมูลเดิมถูกต้อง

2. เปลี่ยนเกม
   → กรองรายการเติมตามเกมที่เลือก
   → รีเซ็ตรายการเติม

3. เปลี่ยนรายการเติม
   → แสดงรายละเอียดรายการใหม่
   → คำนวณยอดรวมใหม่

4. เพิ่มจำนวน
   → คำนวณต้นทุน ราคาขาย กำไรใหม่

5. เพิ่มส่วนลด
   → คำนวณยอดสุทธิและกำไรใหม่

6. บันทึก
   → อัปเดตข้อมูลสำเร็จ
   → บันทึก Activity Log
   → รีเฟรชหน้าจอ
```

#### ✅ ลบยอดขาย
```
1. คลิกปุ่ม More (⋮)
   → แสดง dropdown menu

2. คลิก "ลบ"
   → แสดง confirmation dialog

3. ยืนยัน
   → ลบยอดขายสำเร็จ
   → บันทึก Activity Log
   → รีเฟรชหน้าจอ
```

#### ✅ จำนวนที่ขาย
```
1. มียอดขายวันนี้
   → แสดงจำนวนชิ้นถูกต้อง

2. เพิ่มยอดขาย
   → จำนวนเพิ่มขึ้น

3. แก้ไขจำนวน
   → จำนวนเปลี่ยนตาม

4. ลบยอดขาย
   → จำนวนลดลง
```

---

## 🐛 Troubleshooting

### ปัญหา: ปุ่มแก้ไขไม่ทำงาน

**สาเหตุ:**
- Sale data ไม่ครบ
- Games หรือ gameItems ไม่โหลด

**แก้ไข:**
```typescript
// ตรวจสอบใน console
console.log("Sale:", sale);
console.log("Games:", games);
console.log("Game Items:", gameItems);
```

### ปัญหา: จำนวนที่ขายไม่ถูกต้อง

**สาเหตุ:**
- ข้อมูลยอดขายยังไม่รีเฟรช
- คำนวณไม่ถูกต้อง

**แก้ไข:**
```typescript
// ตรวจสอบการคำนวณ
const todayQuantity = todaySalesData.reduce(
  (sum, sale) => sum + sale.quantity, 
  0
);
console.log("Today Quantity:", todayQuantity);
```

### ปัญหา: การแก้ไขไม่บันทึก

**สาเหตุ:**
- Validation ไม่ผ่าน
- Error จาก Firestore

**แก้ไข:**
```typescript
// ดูใน console
try {
  const result = await updateSale(...);
  console.log("Update result:", result);
} catch (error) {
  console.error("Update error:", error);
}
```

---

## 💡 Best Practices

### 1. การแก้ไขยอดขาย
- ตรวจสอบข้อมูลเดิมก่อนแก้ไข
- แก้ไขทีละรายการ
- ดูสรุปยอดก่อนบันทึก

### 2. การลบยอดขาย
- ตรวจสอบความถูกต้องก่อนลบ
- ลบเฉพาะยอดที่ผิดพลาดหรือซ้ำซ้อน
- พิจารณาใช้แก้ไขแทนการลบ

### 3. การดูสถิติ
- ดูจำนวนที่ขายประกอบยอดขาย
- เปรียบเทียบกับวันก่อนหน้า
- วิเคราะห์แนวโน้ม

---

## 🎯 สรุป

### ✅ สิ่งที่ได้รับการอัปเดต:

1. **แก้ไขยอดขาย** ✏️
   - Dialog สวยงาม ใช้งานง่าย
   - คำนวณอัตโนมัติ
   - Validation ครบถ้วน

2. **ลบยอดขาย** 🗑️
   - Confirmation ก่อนลบ
   - Activity Log
   - UI ปรับปรุง (Dropdown Menu)

3. **จำนวนที่ขาย** 📦
   - StatCard ใหม่
   - คำนวณเรียลไทม์
   - แสดงชิ้นที่ขายวันนี้

### 📈 ผลลัพธ์:

- ✅ ระบบครบถ้วนมากขึ้น
- ✅ จัดการยอดขายได้ยืดหยุ่น
- ✅ ดูสถิติได้ละเอียดขึ้น
- ✅ UI/UX ดีขึ้น
- ✅ Activity Logging ครบถ้วน

---

**Version:** 1.0  
**Date:** October 2024  
**Author:** Game Nexus Dashboard Team

🎉 **ระบบยอดขายพร้อมใช้งานครบถ้วนแล้ว!**



