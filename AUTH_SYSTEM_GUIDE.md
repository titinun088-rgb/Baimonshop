# 🔐 ระบบสมัคร / ล็อกอิน / ยืนยันอีเมล - คู่มือการใช้งาน

## ✅ สิ่งที่สร้างเสร็จสมบูรณ์

### 1. 📄 หน้าเว็บที่สร้างใหม่

#### `/register` - หน้าสมัครสมาชิก
- ✅ ฟอร์มสมัครสมาชิกพร้อม validation
- ✅ ฟิลด์: ชื่อร้าน, อีเมล, รหัสผ่าน, ยืนยันรหัสผ่าน
- ✅ ตรวจสอบรหัสผ่านอย่างน้อย 6 ตัวอักษร
- ✅ ตรวจสอบความตรงกันของรหัสผ่าน
- ✅ บันทึกข้อมูลใน Firebase Auth + Firestore
- ✅ **ส่งอีเมลยืนยันอัตโนมัติหลังสมัคร**
- ✅ Redirect ไป `/verify-email` หลังสมัครสำเร็จ

#### `/login` - หน้าเข้าสู่ระบบ
- ✅ เข้าสู่ระบบด้วย Email/Password
- ✅ เข้าสู่ระบบด้วย Google OAuth
- ✅ ลิงก์ไปหน้าลืมรหัสผ่าน
- ✅ ลิงก์ไปหน้าสมัครสมาชิก
- ✅ Error handling แบบละเอียด (แปลเป็นภาษาไทย)

#### `/verify-email` - หน้ายืนยันอีเมล
- ✅ แสดงสถานะการยืนยันอีเมล
- ✅ คำแนะนำขั้นตอนการยืนยัน
- ✅ ปุ่ม "ส่งอีเมลอีกครั้ง" พร้อม cooldown 60 วินาที
- ✅ ปุ่ม "ตรวจสอบสถานะ" เพื่อ refresh สถานะ
- ✅ **บล็อกการเข้าถึงหน้าอื่นๆ จนกว่าจะยืนยันอีเมล**
- ✅ Auto-redirect ไป dashboard เมื่อยืนยันสำเร็จ

#### `/forgot-password` - หน้ารีเซ็ตรหัสผ่าน
- ✅ ฟอร์มกรอกอีเมล
- ✅ ส่งลิงก์รีเซ็ตรหัสผ่านทางอีเมล
- ✅ หน้าแจ้งผลสำเร็จพร้อมคำแนะนำ
- ✅ ปุ่มส่งอีกครั้ง

---

## 🔥 Firebase Integration

### AuthContext Features
```typescript
// ฟังก์ชันที่มีให้ใช้งาน
const {
  user,              // ข้อมูล Firebase User
  userData,          // ข้อมูลจาก Firestore
  loading,           // สถานะกำลังโหลด
  signIn,            // ล็อกอิน Email/Password
  signUp,            // สมัครสมาชิก + บันทึก Firestore + ส่งอีเมล
  signInWithGoogle,  // ล็อกอินด้วย Google
  signOut,           // ออกจากระบบ
  sendVerificationEmail,  // ส่งอีเมลยืนยัน
  resetPassword,     // ส่งลิงก์รีเซ็ตรหัสผ่าน
  refreshUser,       // รีเฟรชสถานะ user
} = useAuth();
```

### Firestore Schema
```typescript
// Collection: users/{uid}
{
  uid: string;
  email: string;
  displayName: string;
  shopName: string;
  createdAt: Date;
  emailVerified: boolean;
  photoURL?: string;
}
```

---

## 🛡️ Protected Route System

### ระดับการป้องกัน

1. **Public Routes** - เข้าได้โดยไม่ต้องล็อกอิน
   - `/login`
   - `/register`
   - `/forgot-password`

2. **Semi-Protected** - ต้องล็อกอินแต่ไม่ต้องยืนยันอีเมล
   - `/verify-email`

3. **Fully Protected** - ต้องล็อกอิน + ยืนยันอีเมล
   - `/` (Dashboard)
   - `/games`
   - `/sales`
   - `/users`
   - `/activity`
   - `/notifications`

### กลไกการทำงาน

```
ผู้ใช้เข้าถึงหน้า Protected
        ↓
  ตรวจสอบล็อกอินหรือยัง?
        ↓
   [ไม่] → Redirect to /login
        ↓
   [ใช่] → ยืนยันอีเมลหรือยัง?
        ↓
   [ไม่] → Redirect to /verify-email
        ↓
   [ใช่] → แสดงหน้าที่ต้องการ ✅
```

---

## 🎯 User Flow

### 1. การสมัครสมาชิกใหม่

```
ผู้ใช้ → /register
   ↓
กรอกข้อมูล (ชื่อร้าน, อีเมล, รหัสผ่าน)
   ↓
คลิก "สมัครสมาชิก"
   ↓
✅ สร้าง User ใน Firebase Auth
✅ บันทึกข้อมูลใน Firestore
✅ ส่งอีเมลยืนยันอัตโนมัติ
   ↓
Redirect → /verify-email
   ↓
ผู้ใช้เปิดอีเมล → คลิกลิงก์ยืนยัน
   ↓
กลับมาที่เว็บ → คลิก "ตรวจสอบสถานะ"
   ↓
✅ Verified → Redirect to Dashboard
```

### 2. การล็อกอินด้วย Email/Password

```
ผู้ใช้ → /login
   ↓
กรอกอีเมล + รหัสผ่าน
   ↓
คลิก "เข้าสู่ระบบ"
   ↓
ตรวจสอบ emailVerified?
   ↓
[ไม่] → Redirect to /verify-email
   ↓
[ใช่] → Redirect to Dashboard ✅
```

### 3. การล็อกอินด้วย Google

```
ผู้ใช้ → /login
   ↓
คลิก "เข้าสู่ระบบด้วย Google"
   ↓
เลือกบัญชี Google
   ↓
✅ Google users มี emailVerified = true อัตโนมัติ
✅ สร้างข้อมูลใน Firestore (ถ้ายังไม่มี)
   ↓
Redirect to Dashboard ✅
```

### 4. การลืมรหัสผ่าน

```
ผู้ใช้ → /forgot-password
   ↓
กรอกอีเมล
   ↓
คลิก "ส่งลิงก์รีเซ็ตรหัสผ่าน"
   ↓
✅ Firebase ส่งอีเมลพร้อมลิงก์
   ↓
ผู้ใช้เปิดอีเมล → คลิกลิงก์
   ↓
ตั้งรหัสผ่านใหม่
   ↓
กลับมา /login → เข้าสู่ระบบ ✅
```

---

## 🔧 Firebase Console Setup

### 1. เปิดใช้งาน Authentication Methods

1. ไปที่ [Firebase Console](https://console.firebase.google.com/)
2. เลือกโปรเจกต์ `game-shop-72ad1`
3. **Authentication** > **Sign-in method**
4. เปิดใช้งาน:

   ✅ **Email/Password**
   - เปิดใช้งาน "Email/Password"
   - (ไม่ต้องเปิด "Email link")

   ✅ **Google**
   - เปิดใช้งาน "Google"
   - กรอก Project public-facing name: `Game Shop`
   - กรอก Support email: `your-email@example.com`

### 2. ตั้งค่า Authorized Domains

**Authentication** > **Settings** > **Authorized domains**

เพิ่มโดเมนที่ใช้งาน:
- ✅ `localhost` (สำหรับการพัฒนา)
- ✅ โดเมนจริงของคุณเมื่อ deploy (เช่น `yourapp.vercel.app`)

### 3. ตั้งค่า Email Templates (Optional)

**Authentication** > **Templates**

แก้ไขเทมเพลตอีเมลสำหรับ:
- Email address verification
- Password reset
- Email address change

สามารถ customize:
- From name: `Game Shop`
- Subject
- เนื้อหาข้อความ

### 4. ตั้งค่า Firestore Rules

**Firestore Database** > **Rules**

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // อนุญาตให้ user อ่าน/เขียนข้อมูลของตัวเองเท่านั้น
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

---

## 🚀 วิธีใช้งาน

### 1. เริ่มต้น Development Server

```bash
npm run dev
```

เปิด `http://localhost:8080`

### 2. ทดสอบระบบ

#### ทดสอบสมัครสมาชิก
1. ไปที่ `/register`
2. กรอกข้อมูล:
   - ชื่อร้าน: `Test Shop`
   - อีเมล: `test@example.com`
   - รหัสผ่าน: `password123`
3. คลิก "สมัครสมาชิก"
4. ✅ ควรถูก redirect ไป `/verify-email`
5. ✅ ควรได้รับอีเมลยืนยัน

#### ทดสอบยืนยันอีเมล
1. เปิดกล่องจดหมาย
2. คลิกลิงก์ยืนยันในอีเมล
3. กลับมาที่เว็บ
4. คลิก "ตรวจสอบสถานะ"
5. ✅ ควรถูก redirect ไป Dashboard

#### ทดสอบล็อกอิน
1. ไปที่ `/login`
2. กรอกอีเมล + รหัสผ่าน
3. คลิก "เข้าสู่ระบบ"
4. ✅ ถ้ายืนยันอีเมลแล้ว → Dashboard
5. ✅ ถ้ายังไม่ยืนยัน → `/verify-email`

#### ทดสอบ Google Login
1. ไปที่ `/login`
2. คลิก "เข้าสู่ระบบด้วย Google"
3. เลือกบัญชี Google
4. ✅ ควรเข้าสู่ Dashboard โดยตรง (Google verified อัตโนมัติ)

#### ทดสอบรีเซ็ตรหัสผ่าน
1. ไปที่ `/forgot-password`
2. กรอกอีเมล
3. คลิก "ส่งลิงก์รีเซ็ตรหัสผ่าน"
4. ✅ ควรได้รับอีเมลรีเซ็ต
5. คลิกลิงก์ในอีเมล
6. ตั้งรหัสผ่านใหม่

---

## 🎨 UI Components ที่ใช้

### Register Page
- Card (กรอบหลัก)
- Input (ช่องกรอกข้อมูล)
- Label (ป้ายชื่อฟิลด์)
- Button (ปุ่มส่งฟอร์ม)
- Toast (แจ้งเตือน)
- Loader (แสดงขณะโหลด)

### Login Page
- Card
- Input
- Button (ธรรมดา + Google)
- Link (ไปหน้าอื่น)

### Verify Email Page
- Card
- Button (Check Status, Resend)
- Alert/Info box
- Instructions list
- Cooldown timer

### Forgot Password Page
- Card
- Input
- Button
- Success message
- Instructions

---

## 📊 Database Structure

### Firebase Auth
- UID (auto-generated)
- Email
- Password (hashed)
- Display Name
- Email Verified (boolean)
- Photo URL
- Created At
- Last Sign In

### Firestore `users` Collection
```json
{
  "users": {
    "{uid}": {
      "uid": "abc123...",
      "email": "test@example.com",
      "displayName": "Test Shop",
      "shopName": "Test Shop",
      "createdAt": "2024-01-01T00:00:00.000Z",
      "emailVerified": true,
      "photoURL": "https://..."
    }
  }
}
```

---

## ⚠️ จุดสำคัญที่ต้องเข้าใจ

### 1. Email Verification เป็นข้อบังคับ
- ผู้ใช้ที่สมัครด้วย Email/Password **ต้องยืนยันอีเมล**ก่อนใช้งาน
- ถ้ายังไม่ยืนยัน จะถูก redirect ไป `/verify-email`
- Google OAuth users มี `emailVerified = true` อัตโนมัติ

### 2. Cooldown สำหรับส่งอีเมลซ้ำ
- หลังส่งอีเมลยืนยันแล้ว ต้องรอ 60 วินาทีก่อนส่งอีกครั้ง
- ป้องกันการ spam

### 3. Firestore + Auth ทำงานร่วมกัน
- Firebase Auth จัดการ authentication
- Firestore เก็บข้อมูลเพิ่มเติม (ชื่อร้าน, ฯลฯ)
- เมื่อ signUp จะบันทึกทั้ง 2 ที่พร้อมกัน

### 4. Auto-redirect Logic
```
Register → Verify Email → Dashboard
Login (not verified) → Verify Email
Login (verified) → Dashboard
Google Login → Dashboard (verified already)
```

---

## 🐛 Troubleshooting

### ปัญหา: "Firebase: Error (auth/email-already-in-use)"
**สาเหตุ:** อีเมลนี้ถูกใช้สมัครแล้ว  
**แก้ไข:** ใช้อีเมลอื่น หรือล็อกอินแทน

### ปัญหา: "Firebase: Error (auth/popup-blocked)"
**สาเหตุ:** เบราว์เซอร์บล็อก popup  
**แก้ไข:** อนุญาต popup สำหรับ localhost

### ปัญหา: "Firebase: Error (auth/unauthorized-domain)"
**สาเหตุ:** โดเมนไม่ได้รับอนุญาตใน Firebase  
**แก้ไข:** เพิ่มโดเมนใน Firebase Console > Authentication > Authorized domains

### ปัญหา: ไม่ได้รับอีเมลยืนยัน
**แก้ไข:**
1. ตรวจสอบโฟลเดอร์ Spam/Junk
2. ตรวจสอบว่า Firebase Auth เปิดใช้งาน Email/Password แล้ว
3. ตรวจสอบ sender email ใน Firebase Console

### ปัญหา: หลังยืนยันอีเมลแล้วยังถูก redirect ไป /verify-email
**แก้ไข:**
1. คลิกปุ่ม "ตรวจสอบสถานะ" บนหน้า verify-email
2. หรือลอง logout แล้ว login ใหม่
3. ตรวจสอบว่า `user.emailVerified` เป็น `true` หรือยัง (ใน console)

### ปัญหา: "Firebase: Error (auth/too-many-requests)"
**สาเหตุ:** ส่งคำขอบ่อยเกินไป  
**แก้ไข:** รอสักครู่แล้วลองใหม่

---

## 📚 เอกสารเพิ่มเติม

- [Firebase Authentication Docs](https://firebase.google.com/docs/auth)
- [Firebase Email Verification](https://firebase.google.com/docs/auth/web/manage-users#send_a_user_a_verification_email)
- [Firestore Security Rules](https://firebase.google.com/docs/firestore/security/get-started)
- [React Router Protected Routes](https://reactrouter.com/en/main/start/overview)

---

## ✨ สรุป

ระบบ Authentication ครบครันพร้อมใช้งาน:

✅ สมัครสมาชิก (Email + ชื่อร้าน + รหัสผ่าน)  
✅ ล็อกอิน (Email/Password + Google)  
✅ ยืนยันอีเมล (ส่งอัตโนมัติ + ส่งซ้ำได้)  
✅ รีเซ็ตรหัสผ่าน  
✅ บันทึกข้อมูลใน Firestore  
✅ Protected Routes พร้อมตรวจสอบ email verification  
✅ Error handling ภาษาไทย  
✅ UI/UX สวยงาม Gaming theme  

**พร้อมใช้งานจริง 100%!** 🎉

