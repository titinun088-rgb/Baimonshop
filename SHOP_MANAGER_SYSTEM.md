# ระบบผู้ดูแลร้านค้า (Shop Manager System)

## 📋 Overview

ระบบผู้ดูแลร้านค้าช่วยให้เจ้าของร้านสามารถเชิญผู้ใช้อื่นมาเป็นผู้ดูแลร้านค้า (Co-Admin/Manager) เพื่อช่วยจัดการข้อมูลและยอดขายของร้านร่วมกัน

## 🎯 Features

### สำหรับเจ้าของร้าน (Shop Owner) และแอดมิน (Admin)
- ✅ ส่งคำขอเชิญผู้ใช้เป็นผู้ดูแลร้านค้า
- ✅ ดูรายชื่อผู้ดูแลร้านค้าปัจจุบัน
- ✅ ลบผู้ดูแลร้านค้าออก

### สำหรับผู้ใช้ทั่วไป (User)
- ✅ รับคำขอเป็นผู้ดูแลร้านค้า
- ✅ ตอบรับหรือปฏิเสธคำขอ
- ✅ แสดง badge แจ้งเตือนคำขอที่รอการตอบรับ
- ✅ เข้าถึงข้อมูลร้านค้าเมื่อตอบรับคำขอ

## 📁 File Structure

```
src/
├── types/
│   └── shopInvitation.ts          # Type definitions
├── lib/
│   └── shopInvitationUtils.ts     # Utility functions
├── components/
│   └── InviteShopManagerDialog.tsx # Dialog สำหรับส่งคำขอ
├── pages/
│   ├── ShopInvitations.tsx        # หน้าแสดงคำขอที่ได้รับ
│   └── Profile.tsx                # เพิ่มส่วนจัดการผู้ดูแล
└── contexts/
    └── AuthContext.tsx            # อัปเดตให้รองรับ shop members
```

## 🗄️ Firestore Collections

### 1. `shopInvitations`
เก็บคำขอเชิญผู้ดูแลร้านค้า

```typescript
{
  id: string;
  shopOwnerId: string;        // UID ของเจ้าของร้าน
  shopOwnerEmail: string;
  shopName: string;
  invitedUserId: string;      // UID ของผู้ถูกเชิญ
  invitedUserEmail: string;
  status: "pending" | "accepted" | "rejected";
  createdAt: Date;
  updatedAt: Date;
  respondedAt?: Date;
}
```

### 2. `shopMembers`
เก็บรายชื่อผู้ดูแลร้านค้า

```typescript
{
  shopOwnerId: string;        // UID ของเจ้าของร้าน
  memberId: string;           // UID ของผู้ดูแล
  memberEmail: string;
  addedAt: Date;
  role: "co-admin" | "manager";
}
```

## 🚀 How to Use

### 1. การส่งคำขอเชิญผู้ดูแล

#### ขั้นตอนที่ 1: ไปที่หน้าโปรไฟล์
- คลิกที่เมนู **"โปรไฟล์"** ในแถบด้านซ้าย

#### ขั้นตอนที่ 2: เปิด Dialog เพิ่มผู้ดูแล
- หาส่วน **"ผู้ดูแลร้านค้า"** (เฉพาะเจ้าของร้านและแอดมิน)
- คลิกปุ่ม **"เพิ่มผู้ดูแล"**

#### ขั้นตอนที่ 3: กรอก Email และส่งคำขอ
- กรอก email ของผู้ใช้ที่ต้องการเชิญ
- คลิก **"ส่งคำขอ"**

**หมายเหตุ:**
- ผู้ใช้ต้องมีบัญชีในระบบก่อน
- ไม่สามารถเชิญตัวเองได้
- ไม่สามารถเชิญเจ้าของร้านอื่นได้ (เฉพาะ user ทั่วไป)
- ไม่สามารถส่งคำขอซ้ำถ้าคำขอยังค้างอยู่

### 2. การตอบรับคำขอ

#### ขั้นตอนที่ 1: ตรวจสอบคำขอ
- ดูที่ badge สีแดงบนเมนู **"คำขอผู้ดูแล"**
- คลิกเข้าไปดูคำขอที่ได้รับ

#### ขั้นตอนที่ 2: ตอบรับหรือปฏิเสธ
- อ่านรายละเอียดคำขอและสิทธิ์ที่จะได้รับ
- คลิก **"ตอบรับ"** เพื่อเป็นผู้ดูแลร้านค้า
- หรือคลิก **"ปฏิเสธ"** เพื่อปฏิเสธคำขอ

#### ขั้นตอนที่ 3: ระบบจะรีโหลดหน้าเว็บ
- เมื่อตอบรับคำขอ ระบบจะรีโหลดหน้าเว็บอัตโนมัติ
- คุณจะสามารถเข้าถึงข้อมูลร้านค้าได้ทันที

### 3. การจัดการผู้ดูแล

#### ดูรายชื่อผู้ดูแลปัจจุบัน
- ไปที่หน้า **"โปรไฟล์"**
- ส่วน **"ผู้ดูแลร้านค้า"** จะแสดงรายชื่อทั้งหมด

#### ลบผู้ดูแลออก
- คลิกปุ่มถังขยะสีแดงข้างชื่อผู้ดูแล
- ยืนยันการลบ
- ผู้ดูแลจะถูกลบออกและไม่สามารถเข้าถึงข้อมูลร้านได้อีก

## 🔑 Permissions & Access

### เจ้าของร้าน (Shop Owner / Seller)
- ✅ เชิญผู้ใช้เป็นผู้ดูแล
- ✅ ลบผู้ดูแลออก
- ✅ เข้าถึงข้อมูลร้านของตัวเอง

### แอดมิน (Admin)
- ✅ เชิญผู้ใช้เป็นผู้ดูแล
- ✅ ลบผู้ดูแลออก
- ✅ เข้าถึงข้อมูลทุกอย่างในระบบ

### ผู้ดูแลร้าน (Shop Manager / Co-Admin)
- ✅ ดูข้อมูลเกมและรายการเติมเงิน
- ✅ บันทึกและจัดการยอดขาย
- ✅ ดูสถิติและรายงานของร้าน
- ✅ จัดการกิจกรรมและการแจ้งเตือน
- ❌ ไม่สามารถเชิญผู้ดูแลคนอื่นได้
- ❌ ไม่สามารถลบผู้ดูแลออกได้

## 🔧 Technical Details

### AuthContext Updates

เพิ่ม properties ใหม่:
```typescript
{
  managedShops: string[];           // รายการ shop owner IDs ที่ดูแล
  currentShopOwnerId: string | null; // ID ของร้านที่กำลังจัดการ
  invitationCount: number;          // จำนวนคำขอที่รอ
  loadInvitationCount: () => Promise<void>; // ฟังก์ชันโหลดคำขอใหม่
}
```

### Utility Functions

#### `sendShopInvitation()`
ส่งคำขอเชิญผู้ดูแล
- ตรวจสอบว่า email มีในระบบ
- ตรวจสอบว่าไม่ซ้ำกับคำขอที่มีอยู่
- สร้าง invitation document

#### `getMyInvitations()`
ดึงคำขอที่ส่งมาหาผู้ใช้

#### `acceptInvitation()`
ตอบรับคำขอ
- อัปเดตสถานะเป็น "accepted"
- เพิ่มเข้า shopMembers collection

#### `rejectInvitation()`
ปฏิเสธคำขอ
- อัปเดตสถานะเป็น "rejected"

#### `getShopMembers()`
ดึงรายชื่อผู้ดูแลร้านทั้งหมด

#### `removeShopMember()`
ลบผู้ดูแลร้านออก

#### `getManagedShops()`
ดึงรายการร้านที่ผู้ใช้เป็นผู้ดูแล

## 📊 UI Components

### Navigation Badge
- แสดงจำนวนคำขอที่รอการตอบรับบนเมนู "คำขอผู้ดูแล"
- สีแดงเพื่อให้เห็นชัดเจน
- หายไปเมื่อตอบรับหรือปฏิเสธคำขอทั้งหมด

### InviteShopManagerDialog
- Dialog สำหรับกรอก email และส่งคำขอ
- แสดงชื่อร้านที่จะเชิญ
- มี validation ครบถ้วน

### ShopInvitations Page
- แสดงรายการคำขอทั้งหมด
- แสดงรายละเอียดและสิทธิ์ที่จะได้รับ
- ปุ่มตอบรับและปฏิเสธ

### Profile Page - Shop Members Section
- แสดงรายชื่อผู้ดูแลปัจจุบัน
- ปุ่มเพิ่มผู้ดูแล
- ปุ่มลบผู้ดูแล (ถังขยะสีแดง)

## 🎨 Design Patterns

### Loading States
- แสดง Loader ขณะโหลดข้อมูล
- Disable ปุ่มขณะกำลังทำงาน

### Error Handling
- Toast notifications สำหรับทุก action
- Error messages ที่ชัดเจน
- Validation ก่อนส่งข้อมูล

### Real-time Updates
- อัปเดต badge ทันทีเมื่อมีคำขอใหม่
- Reload หน้าเว็บเมื่อตอบรับคำขอ
- Reload รายชื่อผู้ดูแลเมื่อเพิ่ม/ลบ

## 🚨 Known Limitations

### ✅ Queries - อัปเดตแล้ว

ทุกหน้าได้รับการอัปเดตให้รองรับ `currentShopOwnerId` แล้ว:

- ✅ Profile.tsx - อัปเดตแล้ว
- ✅ Games.tsx - อัปเดตแล้ว
- ✅ Sales.tsx - อัปเดตแล้ว
- ✅ Dashboard.tsx - อัปเดตแล้ว
- ✅ GameDetail.tsx - อัปเดตแล้ว (รวมการตรวจสอบสิทธิ์)

### Other Limitations
- ผู้ดูแลสามารถดูแลร้านได้เพียงร้านเดียวในเวลานี้ (ไม่มีการสลับร้าน)
- ไม่มีระบบ permission แบบละเอียด (ทุกคนมีสิทธิ์เท่ากัน)
- ไม่มีประวัติการกระทำของผู้ดูแล

## 🔮 Future Enhancements

### Phase 2 - Advanced Features
- [ ] สลับระหว่างร้านที่ดูแลได้หลายร้าน
- [ ] ระบบ permission แบบละเอียด (read-only, full-access, etc.)
- [ ] ประวัติการกระทำของผู้ดูแล (Activity Log)
- [ ] การแจ้งเตือนผ่าน email เมื่อได้รับคำขอ
- [ ] Dashboard แสดงผู้ดูแลทั้งหมดในระบบ (สำหรับ Admin)

### Phase 3 - Enterprise Features
- [ ] ระบบ Teams และ Departments
- [ ] การจำกัดสิทธิ์ตาม IP Address
- [ ] Two-Factor Authentication สำหรับผู้ดูแล
- [ ] Audit Trail แบบละเอียด
- [ ] การตั้งค่า Session Timeout

## 📝 Changelog

### v1.0.1 (Current)
- ✅ แก้ไขปัญหาข้อมูลไม่ขึ้นหลังตอบรับคำขอ
- ✅ เพิ่ม console.log สำหรับ debug currentShopOwnerId
- ✅ ปรับปรุง flow การตอบรับคำขอให้รอ Firestore sync
- ✅ Navigate ไป Dashboard แทนการ reload หน้า

### v1.0.0
- ✅ ระบบส่งและตอบรับคำขอ
- ✅ จัดการรายชื่อผู้ดูแล
- ✅ Badge แจ้งเตือนคำขอ
- ✅ UI ครบถ้วนทุกหน้า
- ✅ Queries รองรับ currentShopOwnerId แล้ว

## 🆘 Troubleshooting

### คำขอไม่แสดง
- ตรวจสอบว่า email ถูกต้อง
- ตรวจสอบว่าผู้ใช้มีบัญชีในระบบ
- ลอง refresh หน้าเว็บ

### ไม่สามารถส่งคำขอได้
- ตรวจสอบว่าไม่ได้ส่งคำขอไปแล้ว
- ตรวจสอบว่าผู้ใช้ไม่ได้เป็นผู้ดูแลอยู่แล้ว
- ตรวจสอบว่า email เป็น user ทั่วไป ไม่ใช่ shop owner

### ข้อมูลไม่ขึ้นหลังตอบรับคำขอ (แก้ไขแล้ว v1.0.1)
- ระบบจะรอให้ Firestore sync (1 วินาที)
- จะ refresh user data อัตโนมัติ
- จะ navigate ไป Dashboard เพื่อแสดงข้อมูล
- ตรวจสอบ Console (F12) สำหรับ debug logs:
  - "✅ User is shop manager, using shop owner ID: xxx"
  - "🔄 Refreshing user data after accepting invitation..."
  - "✅ Navigating to dashboard..."

### Badge ไม่หายหลังตอบรับ
- ระบบจะอัปเดตอัตโนมัติหลัง navigate
- ถ้ายังไม่หาย ลอง refresh หน้า
- ตรวจสอบ console สำหรับ error

## 📞 Support

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ Console (F12) สำหรับ error messages
2. ดูเอกสาร DEBUG_GUIDE.md
3. ติดต่อทีมพัฒนา

---

**Last Updated:** 2025-01-XX  
**Version:** 1.0.1  
**Status:** ✅ Production Ready

