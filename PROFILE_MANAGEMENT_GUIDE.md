# 👤 ระบบจัดการโปรไฟล์ผู้ใช้ (Profile Management System)

## 📋 ภาพรวม

ระบบจัดการโปรไฟล์ช่วยให้ผู้ใช้สามารถแก้ไขข้อมูลส่วนตัว เปลี่ยนรหัสผ่าน และจัดการรูปโปรไฟล์ได้ด้วยตัวเอง

---

## ✨ คุณสมบัติหลัก

### 1. **แก้ไขข้อมูลโปรไฟล์**
- ✅ เปลี่ยนชื่อที่แสดง (Display Name)
- ✅ แก้ไขชื่อร้าน (Shop Name)
- ✅ เปลี่ยนรูปโปรไฟล์ผ่าน URL
- ✅ ตัวอย่างรูปแบบเรียลไทม์

### 2. **เปลี่ยนรหัสผ่าน**
- ✅ ระบุรหัสผ่านปัจจุบัน (สำหรับยืนยันตัวตน)
- ✅ กำหนดรหัสผ่านใหม่
- ✅ ยืนยันรหัสผ่านใหม่
- ✅ ตรวจสอบความปลอดภัย (ต้องมีอย่างน้อย 6 ตัวอักษร)

### 3. **ดูข้อมูลบัญชี**
- ✅ แสดงอีเมล
- ✅ แสดงบทบาท (Admin/Seller)
- ✅ วันที่สมัครสมาชิก
- ✅ สถานะการยืนยันอีเมล
- ✅ สถานะการอนุมัติบัญชี

### 4. **Activity Logging**
- ✅ บันทึกการอัปเดตโปรไฟล์
- ✅ บันทึกการเปลี่ยนรหัสผ่าน
- ✅ ตรวจสอบย้อนหลังได้

---

## 🔧 โครงสร้างไฟล์

### ไฟล์ใหม่ที่สร้าง:
```
src/
├── lib/
│   └── profileUtils.ts          # ฟังก์ชันจัดการโปรไฟล์
├── pages/
│   └── Profile.tsx              # หน้าโปรไฟล์
└── components/
    └── Layout.tsx (แก้ไข)       # เพิ่มลิงก์โปรไฟล์
```

### ไฟล์ที่แก้ไข:
```
src/
├── App.tsx                       # เพิ่ม route /profile
└── components/
    └── Layout.tsx                # เพิ่มเมนูโปรไฟล์
```

---

## 📖 การใช้งาน

### เข้าสู่หน้าโปรไฟล์

**วิธีที่ 1: จากเมนู**
1. คลิกที่ **"โปรไฟล์"** ในเมนูด้านซ้าย
2. หรือไปที่ URL: `/profile`

**วิธีที่ 2: จาก User Section**
1. คลิกที่ **ชื่อผู้ใช้** ด้านล่างเมนู
2. จะไปที่หน้าโปรไฟล์โดยอัตโนมัติ

---

## 🎨 ส่วนประกอบหลัก

### 1. Profile Summary (ซ้ายมือ)

แสดงข้อมูลสรุปของผู้ใช้:
- รูปโปรไฟล์ (Avatar)
- ชื่อที่แสดงและชื่อร้าน
- Badge บทบาท (Admin/Seller)
- อีเมล
- วันที่สมัครสมาชิก
- สถานะการยืนยันอีเมล
- สถานะการอนุมัติบัญชี

### 2. Edit Profile Card (ขวามือบน)

ฟอร์มแก้ไขข้อมูล:
- **ชื่อที่แสดง**: ชื่อที่จะแสดงในระบบ
- **ชื่อร้าน**: ชื่อร้านค้าของคุณ
- **URL รูปโปรไฟล์**: ลิงก์รูปจากอินเทอร์เน็ต
- **ตัวอย่าง**: แสดง preview รูปแบบเรียลไทม์

### 3. Change Password Card (ขวามือล่าง)

ฟอร์มเปลี่ยนรหัสผ่าน:
- **รหัสผ่านปัจจุบัน**: สำหรับยืนยันตัวตน
- **รหัสผ่านใหม่**: ต้องมีอย่างน้อย 6 ตัวอักษร
- **ยืนยันรหัสผ่านใหม่**: ต้องตรงกับรหัสผ่านใหม่

---

## 💻 ตัวอย่างการใช้งาน

### 1. แก้ไขข้อมูลโปรไฟล์

```typescript
// ใน profileUtils.ts
await updateUserProfile({
  userId: "user123",
  displayName: "ชื่อใหม่",
  shopName: "ร้านค้าใหม่",
  photoURL: "https://example.com/avatar.jpg",
  email: "user@example.com"
});
```

**ขั้นตอน:**
1. กรอกชื่อที่ต้องการเปลี่ยน
2. กรอกชื่อร้านใหม่
3. ใส่ URL รูปโปรไฟล์ (ถ้าต้องการ)
4. ดูตัวอย่างในส่วน Preview
5. คลิก **"บันทึกการเปลี่ยนแปลง"**
6. ระบบจะอัปเดตและรีเฟรชข้อมูลอัตโนมัติ

### 2. เปลี่ยนรหัสผ่าน

```typescript
// ใน profileUtils.ts
await changePassword(
  "รหัสผ่านปัจจุบัน",
  "รหัสผ่านใหม่",
  "user@example.com"
);
```

**ขั้นตอน:**
1. กรอก **รหัสผ่านปัจจุบัน**
2. กรอก **รหัสผ่านใหม่** (อย่างน้อย 6 ตัวอักษร)
3. กรอก **ยืนยันรหัสผ่านใหม่** (ต้องตรงกัน)
4. คลิก **"เปลี่ยนรหัสผ่าน"**
5. ระบบจะล็อกเอาท์อัตโนมัติ
6. ล็อกอินใหม่ด้วยรหัสผ่านที่เปลี่ยนแปลง

---

## 🖼️ การใช้ URL รูปโปรไฟล์

### แหล่งรูปที่แนะนำ:

#### 1. **Firebase Storage** (แนะนำที่สุด)
```
https://firebasestorage.googleapis.com/v0/b/your-project.appspot.com/o/avatars%2Fuser.jpg?alt=media
```

#### 2. **Imgur**
```
https://i.imgur.com/abc123.jpg
```

#### 3. **Cloudinary**
```
https://res.cloudinary.com/your-cloud/image/upload/v1234567890/avatar.jpg
```

#### 4. **Gravatar**
```
https://www.gravatar.com/avatar/hash?s=200
```

### ข้อกำหนด URL:
- ✅ ต้องขึ้นต้นด้วย `http://` หรือ `https://`
- ✅ สามารถเข้าถึงได้จากอินเทอร์เน็ต
- ✅ แนะนำ: `.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`, `.svg`
- ⚠️ หลีกเลี่ยง: รูปจากเครื่องเครื่อง (`file://` หรือ `C:/`)

### ตัวอย่างการตรวจสอบ URL:

```typescript
import { isValidImageUrl } from "@/lib/profileUtils";

// ถูกต้อง ✅
isValidImageUrl("https://example.com/avatar.jpg");  // true
isValidImageUrl("https://i.imgur.com/abc.png");     // true

// ไม่ถูกต้อง ❌
isValidImageUrl("C:/Users/avatar.jpg");             // false
isValidImageUrl("file:///avatar.jpg");              // false
isValidImageUrl("not-a-url");                       // false
```

---

## 🔐 ความปลอดภัย

### Re-authentication
เมื่อเปลี่ยนรหัสผ่าน ระบบจะทำการ **re-authenticate** เพื่อยืนยันตัวตน:

```typescript
const credential = EmailAuthProvider.credential(
  user.email,
  currentPassword
);
await reauthenticateWithCredential(user, credential);
```

### การตรวจสอบรหัสผ่าน
- ต้องมีอย่างน้อย **6 ตัวอักษร**
- ต้องกรอกรหัสผ่านปัจจุบันที่ถูกต้อง
- รหัสผ่านใหม่และยืนยันต้องตรงกัน

### Error Handling
```typescript
// auth/wrong-password
"รหัสผ่านปัจจุบันไม่ถูกต้อง"

// auth/weak-password
"รหัสผ่านใหม่ต้องมีอย่างน้อย 6 ตัวอักษร"

// auth/requires-recent-login
"กรุณาล็อกอินใหม่ก่อนเปลี่ยนรหัสผ่าน"
```

---

## 📊 Activity Logging

### การอัปเดตโปรไฟล์
```json
{
  "action": "profile_updated",
  "details": "อัปเดตข้อมูลโปรไฟล์",
  "metadata": {
    "updates": {
      "displayName": "ชื่อใหม่",
      "shopName": "ร้านใหม่",
      "photoURL": "https://..."
    }
  }
}
```

### การเปลี่ยนรหัสผ่าน
```json
{
  "action": "password_changed",
  "details": "เปลี่ยนรหัสผ่านสำเร็จ"
}
```

---

## 🎨 UI Components

### Avatar Component
แสดงรูปโปรไฟล์ พร้อม fallback เป็นตัวอักษรแรก:

```tsx
<Avatar className="h-24 w-24">
  <AvatarImage src={photoURL} alt={displayName} />
  <AvatarFallback>
    {displayName?.charAt(0)?.toUpperCase() || "U"}
  </AvatarFallback>
</Avatar>
```

### Badge Components
แสดงสถานะต่างๆ:

```tsx
// Admin Badge
<Badge variant="default" className="bg-primary">
  <Shield className="mr-1 h-3 w-3" />
  Admin
</Badge>

// Verification Badge
<Badge variant="outline" className="text-success border-success">
  ยืนยันแล้ว
</Badge>
```

---

## 🔄 Data Flow

```
┌─────────────────┐
│ หน้า Profile     │
└────────┬────────┘
         │
         ├──► อ่านข้อมูลจาก AuthContext
         │
         ├──► แสดง Profile Summary
         │
         ├──► ผู้ใช้แก้ไขข้อมูล
         │
         ├──► updateUserProfile() → Firebase
         │    ├──► อัปเดต Auth Profile
         │    ├──► อัปเดต Firestore
         │    └──► บันทึก Activity Log
         │
         ├──► refreshUser() → รีเฟรชข้อมูล
         │
         └──► แสดงข้อความสำเร็จ

┌─────────────────┐
│ เปลี่ยนรหัสผ่าน  │
└────────┬────────┘
         │
         ├──► Re-authenticate (ยืนยันตัวตน)
         │
         ├──► changePassword() → Firebase
         │    ├──► updatePassword()
         │    └──► บันทึก Activity Log
         │
         ├──► แสดงข้อความสำเร็จ
         │
         └──► ล็อกเอาท์ (ต้อง login ใหม่)
```

---

## 🧪 การทดสอบ

### Test Cases

#### 1. แก้ไขโปรไฟล์
```
✓ อัปเดตชื่อที่แสดงสำเร็จ
✓ อัปเดตชื่อร้านสำเร็จ
✓ อัปเดต URL รูปสำเร็จ
✓ ตรวจสอบ URL ที่ไม่ถูกต้อง
✓ แสดง preview ตัวอย่างรูป
✓ บันทึก Activity Log
✓ รีเฟรชข้อมูลอัตโนมัติ
```

#### 2. เปลี่ยนรหัสผ่าน
```
✓ ตรวจสอบรหัสผ่านปัจจุบันถูกต้อง
✓ ตรวจสอบรหัสผ่านใหม่ความยาว >= 6
✓ ตรวจสอบรหัสผ่านใหม่ตรงกัน
✓ แสดง error เมื่อรหัสผ่านผิด
✓ บันทึก Activity Log
✓ เปลี่ยนสำเร็จและล็อกเอาท์
```

#### 3. แสดงข้อมูล
```
✓ แสดงรูปโปรไฟล์ถูกต้อง
✓ แสดงชื่อและอีเมลถูกต้อง
✓ แสดง Badge บทบาทถูกต้อง
✓ แสดงวันที่สมัครถูกต้อง
✓ แสดงสถานะการยืนยัน
```

---

## 🐛 Troubleshooting

### ปัญหา: รูปโปรไฟล์ไม่แสดง

**สาเหตุ:**
- URL ไม่ถูกต้อง
- รูปถูกลบหรือย้าย
- CORS policy ไม่อนุญาต

**แก้ไข:**
```typescript
// ตรวจสอบ URL
console.log("Photo URL:", photoURL);
console.log("Is valid:", isValidImageUrl(photoURL));

// ลองเข้า URL ในเบราว์เซอร์
// ตรวจสอบว่ารูปโหลดได้หรือไม่
```

### ปัญหา: เปลี่ยนรหัสผ่านไม่ได้

**สาเหตุ:**
- รหัสผ่านปัจจุบันผิด
- รหัสผ่านใหม่สั้นเกินไป
- ต้อง re-login

**แก้ไข:**
```typescript
// ตรวจสอบ error code
try {
  await changePassword(current, newPass, email);
} catch (error: any) {
  console.log("Error code:", error.code);
  // auth/wrong-password
  // auth/weak-password
  // auth/requires-recent-login
}
```

### ปัญหา: ข้อมูลไม่อัปเดต

**สาเหตุ:**
- Cache ยังไม่ refresh
- Firestore ล่าช้า

**แก้ไข:**
```typescript
// รีเฟรชข้อมูล
await refreshUser();

// หรือ reload หน้า
window.location.reload();
```

---

## 🎯 Best Practices

### 1. URL รูปโปรไฟล์
- ใช้ CDN หรือ cloud storage (Firebase, Cloudinary, Imgur)
- เก็บรูปขนาดเหมาะสม (300x300 - 500x500 px)
- ใช้รูปที่มีขนาดไฟล์เล็ก (<500KB)
- เลือกรูปที่มี aspect ratio 1:1 (สี่เหลี่ยมจัตุรัส)

### 2. รหัสผ่าน
- ใช้รหัสผ่านที่ปลอดภัย (ผสมตัวอักษร ตัวเลข สัญลักษณ์)
- อย่าใช้รหัสผ่านเดียวกันกับเว็บอื่น
- เปลี่ยนรหัสผ่านเป็นประจำ (3-6 เดือน)

### 3. การอัปเดตข้อมูล
- ตรวจสอบข้อมูลก่อนบันทึก
- แสดง preview ก่อนยืนยัน
- แจ้งเตือนเมื่อสำเร็จหรือผิดพลาด
- บันทึก Activity Log เพื่อตรวจสอบย้อนหลัง

### 4. UX
- แสดงสถานะ loading เมื่อกำลังประมวลผล
- Disable form ขณะกำลังบันทึก
- แสดงข้อความ error ที่เข้าใจง่าย
- รีเฟรชข้อมูลอัตโนมัติหลังอัปเดต

---

## 📚 API Reference

### updateUserProfile()
อัปเดตข้อมูลโปรไฟล์ผู้ใช้

```typescript
async function updateUserProfile(data: {
  userId: string;
  displayName?: string;
  shopName?: string;
  photoURL?: string;
  email?: string;
}): Promise<{ success: boolean; error?: string }>
```

**Parameters:**
- `userId`: ID ของผู้ใช้
- `displayName`: ชื่อที่แสดง (optional)
- `shopName`: ชื่อร้าน (optional)
- `photoURL`: URL รูปโปรไฟล์ (optional)
- `email`: อีเมล (optional, สำหรับ Activity Log)

**Returns:**
- `success`: true ถ้าสำเร็จ, false ถ้าผิดพลาด
- `error`: ข้อความ error (ถ้ามี)

### changePassword()
เปลี่ยนรหัสผ่าน

```typescript
async function changePassword(
  currentPassword: string,
  newPassword: string,
  userEmail: string
): Promise<{ success: boolean; error?: string }>
```

**Parameters:**
- `currentPassword`: รหัสผ่านปัจจุบัน
- `newPassword`: รหัสผ่านใหม่
- `userEmail`: อีเมลผู้ใช้

**Returns:**
- `success`: true ถ้าสำเร็จ, false ถ้าผิดพลาด
- `error`: ข้อความ error (ถ้ามี)

### isValidImageUrl()
ตรวจสอบความถูกต้องของ URL รูปภาพ

```typescript
function isValidImageUrl(url: string): boolean
```

**Parameters:**
- `url`: URL ที่ต้องการตรวจสอบ

**Returns:**
- `true`: ถ้า URL ถูกต้อง
- `false`: ถ้า URL ไม่ถูกต้อง

---

## 💡 Tips & Tricks

### 1. การใช้ Gravatar
Gravatar เป็นบริการรูปโปรไฟล์ที่ผูกกับอีเมล:

```typescript
import md5 from 'crypto-js/md5';

function getGravatarUrl(email: string, size: number = 200): string {
  const hash = md5(email.toLowerCase().trim());
  return `https://www.gravatar.com/avatar/${hash}?s=${size}&d=identicon`;
}

// ใช้งาน
const photoURL = getGravatarUrl(user.email);
```

### 2. อัปโหลดไปFirebase Storage
```typescript
import { ref, uploadBytes, getDownloadURL } from "firebase/storage";
import { storage } from "@/lib/firebase";

async function uploadAvatar(file: File, userId: string) {
  const storageRef = ref(storage, `avatars/${userId}`);
  await uploadBytes(storageRef, file);
  const url = await getDownloadURL(storageRef);
  return url;
}
```

### 3. Lazy Load Avatar
ใช้ `loading="lazy"` เพื่อโหลดรูปเมื่อจำเป็น:

```tsx
<img
  src={photoURL}
  alt={displayName}
  loading="lazy"
  onError={(e) => {
    e.currentTarget.src = "/placeholder-avatar.png";
  }}
/>
```

---

## 📞 สรุป

ระบบจัดการโปรไฟล์ช่วยให้ผู้ใช้:

✅ **แก้ไขข้อมูล** - ชื่อ, ร้าน, รูปโปรไฟล์  
✅ **เปลี่ยนรหัสผ่าน** - ปลอดภัยด้วย re-authentication  
✅ **ดูสถานะบัญชี** - Role, วันที่, การยืนยัน  
✅ **Activity Logging** - บันทึกทุกการเปลี่ยนแปลง  
✅ **UI สวยงาม** - ใช้งานง่าย responsive  

---

**Version:** 1.0  
**Last Updated:** October 2024  
**Author:** Game Nexus Dashboard Team

