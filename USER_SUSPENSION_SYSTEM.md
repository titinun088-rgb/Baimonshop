# 🚫 ระบบพักบัญชีผู้ใช้ (User Suspension System)

## 📋 ภาพรวม

ระบบพักบัญชีผู้ใช้ช่วยให้ Admin สามารถพักการใช้งานบัญชีของผู้ใช้ได้ชั่วคราวหรือถาวร พร้อมระบุเหตุผลและกำหนดระยะเวลา

---

## ✨ คุณสมบัติหลัก

### 1. **การพักบัญชีแบบต่างๆ**
- ✅ **พักชั่วคราว** - กำหนดวันที่สิ้นสุดการพัก ระบบจะปลดพักอัตโนมัติเมื่อถึงเวลา
- ✅ **พักถาวร** - พักจนกว่า Admin จะมาปลดพักด้วยตนเอง

### 2. **ข้อมูลการพัก**
- เหตุผลในการพักบัญชี (บังคับกรอก)
- วันที่เริ่มพัก
- วันที่สิ้นสุดการพัก (ถ้าเป็นการพักชั่วคราว)

### 3. **การป้องกันการเข้าถึง**
- ผู้ใช้ที่ถูกพักจะไม่สามารถเข้าถึงระบบได้
- แสดงหน้าแจ้งเตือนพร้อมข้อมูลการพัก
- Admin ยังคงเข้าใช้งานได้แม้ถูกพัก (เพื่อป้องกันการ lock ตัวเอง)

### 4. **การปลดพักอัตโนมัติ**
- ระบบจะตรวจสอบวันหมดอายุเมื่อผู้ใช้เข้าสู่ระบบ
- ปลดพักอัตโนมัติถ้าครบกำหนด
- บันทึก Activity Log เมื่อมีการพักหรือปลดพัก

---

## 🔧 โครงสร้างข้อมูล

### UserData Interface
```typescript
interface UserData {
  uid: string;
  email: string;
  displayName: string;
  shopName: string;
  role: UserRole;
  verified: boolean;
  emailVerified: boolean;
  suspended: boolean;              // สถานะการพัก
  suspendedUntil?: Date;           // วันที่หมดอายุการพัก (undefined = ถาวร)
  suspendReason?: string;          // เหตุผลที่พัก
  createdAt: Date;
  photoURL?: string;
}
```

### Firestore Structure
```
users/{userId}
  ├── uid: string
  ├── email: string
  ├── shopName: string
  ├── role: "admin" | "seller"
  ├── verified: boolean
  ├── emailVerified: boolean
  ├── suspended: boolean
  ├── suspendedUntil: Timestamp | null
  ├── suspendReason: string | null
  └── createdAt: Timestamp
```

---

## 📖 การใช้งาน

### 1. การพักบัญชีผู้ใช้

#### จากหน้า Users
1. เข้าสู่หน้า **จัดการผู้ใช้** (`/users`)
2. คลิกปุ่ม **⋮** (More) ที่แถวของผู้ใช้ที่ต้องการพัก
3. เลือก **พักบัญชี**
4. เลือกประเภทการพัก:
   - **พักชั่วคราว** - กำหนดวันที่สิ้นสุด
   - **พักถาวร** - พักจนกว่าจะปลดพักเอง
5. กรอกเหตุผลในการพัก (จำเป็น)
6. คลิก **พักบัญชี**

#### ตัวอย่างโค้ด
```typescript
import { suspendUser } from "@/lib/userManagementUtils";

// พักชั่วคราว (7 วัน)
const suspendDate = new Date();
suspendDate.setDate(suspendDate.getDate() + 7);

const result = await suspendUser(
  userId,
  "ละเมิดกฎกติกา",
  suspendDate,
  {
    userId: adminId,
    email: adminEmail,
    shopName: adminShopName,
    targetEmail: userEmail,
  }
);

// พักถาวร
const result = await suspendUser(
  userId,
  "พฤติกรรมไม่เหมาะสม",
  null, // null = พักถาวร
  adminInfo
);
```

### 2. การปลดพักบัญชี

#### จากหน้า Users
1. เข้าสู่หน้า **จัดการผู้ใช้** (`/users`)
2. คลิกปุ่ม **⋮** (More) ที่แถวของผู้ใช้ที่ถูกพัก
3. เลือก **จัดการการพัก**
4. คลิก **ปลดพักบัญชี**
5. ยืนยันการปลดพัก

#### ตัวอย่างโค้ด
```typescript
import { unsuspendUser } from "@/lib/userManagementUtils";

const result = await unsuspendUser(
  userId,
  {
    userId: adminId,
    email: adminEmail,
    shopName: adminShopName,
    targetEmail: userEmail,
  }
);
```

### 3. การตรวจสอบสถานะการพัก

```typescript
import { isUserSuspended } from "@/lib/userManagementUtils";

const status = isUserSuspended(
  user.suspended,
  user.suspendedUntil
);

if (status.isSuspended) {
  console.log("ผู้ใช้ถูกพัก:", status.reason);
} else {
  console.log("ผู้ใช้ไม่ถูกพัก");
}
```

---

## 🔐 การป้องกันและความปลอดภัย

### 1. Protected Route
`ProtectedRoute` จะตรวจสอบสถานะการพักอัตโนมัติ:

```typescript
// ใน ProtectedRoute.tsx
if (userData && userData.suspended && userData.role !== 'admin') {
  const suspensionStatus = isUserSuspended(
    userData.suspended,
    userData.suspendedUntil
  );
  
  if (suspensionStatus.isSuspended) {
    return <Navigate to="/suspended" replace />;
  }
}
```

### 2. หน้า Suspended
ผู้ใช้ที่ถูกพักจะถูกนำไปที่หน้า `/suspended` ซึ่งแสดง:
- ข้อมูลผู้ใช้ที่ถูกพัก
- วันที่สิ้นสุดการพัก (ถ้ามี)
- เหตุผลในการพัก
- ปุ่มออกจากระบบ

### 3. Auto-Unsuspend
ระบบจะตรวจสอบและปลดพักอัตโนมัติเมื่อ:
- ผู้ใช้เข้าสู่ระบบ
- วันที่ปัจจุบัน >= วันที่สิ้นสุดการพัก

```typescript
// ใน ProtectedRoute.tsx
useEffect(() => {
  const checkAndAutoUnsuspend = async () => {
    if (!userData || !userData.suspended || !userData.suspendedUntil) return;

    const suspensionStatus = isUserSuspended(
      userData.suspended,
      userData.suspendedUntil
    );

    if (!suspensionStatus.isSuspended) {
      // ปลดพักอัตโนมัติ
      const userRef = doc(db, "users", userData.uid);
      await updateDoc(userRef, {
        suspended: false,
        suspendedUntil: null,
        suspendReason: null,
      });
      window.location.reload();
    }
  };

  checkAndAutoUnsuspend();
}, [userData]);
```

---

## 📊 Activity Logging

ระบบจะบันทึก Activity Log อัตโนมัติเมื่อ:

### การพักบัญชี
```typescript
await logActivity({
  userId: adminInfo.userId,
  email: adminInfo.email,
  shopName: adminInfo.shopName,
  action: "user_updated",
  details: `พักบัญชี "${targetEmail}" (ถึงวันที่ XX/XX/XXXX) - เหตุผล: ${reason}`,
  metadata: { 
    targetUserId: userId, 
    suspended: true, 
    suspendUntil, 
    reason 
  },
});
```

### การปลดพัก
```typescript
await logActivity({
  userId: adminInfo.userId,
  email: adminInfo.email,
  shopName: adminInfo.shopName,
  action: "user_updated",
  details: `ปลดพักบัญชี "${targetEmail}"`,
  metadata: { 
    targetUserId: userId, 
    suspended: false 
  },
});
```

---

## 🎨 UI Components

### 1. SuspendUserDialog
Dialog สำหรับพัก/ปลดพักบัญชี

**Props:**
```typescript
interface SuspendUserDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  targetUser: {
    uid: string;
    email: string;
    shopName?: string;
    suspended: boolean;
    suspendedUntil?: Date;
    suspendReason?: string;
  } | null;
  onSuccess: () => Promise<void>;
}
```

**Features:**
- Radio buttons สำหรับเลือกประเภทการพัก
- Date picker สำหรับกำหนดวันสิ้นสุด (ถ้าพักชั่วคราว)
- Textarea สำหรับกรอกเหตุผล
- แสดงข้อมูลการพักปัจจุบัน (ถ้ามี)
- ปุ่มปลดพักบัญชี (ถ้าถูกพักอยู่)

### 2. Status Badge in Users Table
แสดง Badge "ถูกพัก" สีแดงในตารางผู้ใช้:

```typescript
{user.suspended ? (
  <Badge variant="outline" className="text-destructive border-destructive">
    <Ban className="mr-1 h-3 w-3" />
    ถูกพัก
  </Badge>
) : (
  // แสดงสถานะปกติ
)}
```

### 3. Dropdown Menu Item
เมนูสำหรับพัก/ปลดพักบัญชีในตาราง:

```typescript
<DropdownMenuItem onClick={() => handleSuspendClick(user)}>
  <Ban className="mr-2 h-4 w-4" />
  {user.suspended ? "จัดการการพัก" : "พักบัญชี"}
</DropdownMenuItem>
```

---

## 🧪 การทดสอบ

### Test Cases

#### 1. พักบัญชีชั่วคราว
```
✓ Admin สามารถพักบัญชีชั่วคราวได้
✓ ต้องระบุวันที่สิ้นสุด
✓ ผู้ใช้ไม่สามารถเข้าระบบได้ขณะถูกพัก
✓ ปลดพักอัตโนมัติเมื่อครบกำหนด
✓ บันทึก Activity Log
```

#### 2. พักบัญชีถาวร
```
✓ Admin สามารถพักบัญชีถาวรได้
✓ ไม่มีวันสิ้นสุด (null)
✓ ผู้ใช้ไม่สามารถเข้าระบบได้
✓ ต้องปลดพักด้วยตัวเองเท่านั้น
✓ บันทึก Activity Log
```

#### 3. การปลดพัก
```
✓ Admin สามารถปลดพักได้ทันที
✓ ล้างข้อมูล suspended, suspendedUntil, suspendReason
✓ ผู้ใช้สามารถเข้าระบบได้อีกครั้ง
✓ บันทึก Activity Log
```

#### 4. Edge Cases
```
✓ Admin ที่ถูกพักยังคงเข้าระบบได้
✓ ตรวจสอบวันหมดอายุเมื่อ login
✓ ปลดพักอัตโนมัติถ้าครบกำหนด
✓ แสดงข้อความที่เหมาะสมในแต่ละกรณี
```

---

## 🐛 Troubleshooting

### ปัญหา: ผู้ใช้ถูกพักแต่ยังเข้าระบบได้

**สาเหตุ:**
- อาจเป็น Admin (Admin bypass suspension check)
- Cache ยังไม่ถูกอัปเดต

**แก้ไข:**
```typescript
// ตรวจสอบว่าเป็น Admin หรือไม่
if (userData.role === 'admin') {
  console.log("Admin can bypass suspension");
}

// รีโหลดข้อมูลผู้ใช้
await refreshUser();
```

### ปัญหา: วันหมดอายุไม่ถูกต้อง

**สาเหตุ:**
- Timezone ไม่ตรงกัน
- Timestamp conversion ผิดพลาด

**แก้ไข:**
```typescript
// ใช้ Timestamp จาก Firestore
import { Timestamp } from "firebase/firestore";

const suspendDate = new Date("2024-12-31");
await updateDoc(userRef, {
  suspendedUntil: Timestamp.fromDate(suspendDate)
});

// อ่านข้อมูล
const data = doc.data();
const suspendedUntil = data.suspendedUntil?.toDate();
```

### ปัญหา: Auto-unsuspend ไม่ทำงาน

**สาเหตุ:**
- useEffect ไม่ถูก trigger
- userData ยังไม่โหลด

**แก้ไข:**
```typescript
// ตรวจสอบใน console
useEffect(() => {
  console.log("Checking suspension:", {
    suspended: userData?.suspended,
    suspendedUntil: userData?.suspendedUntil,
    now: new Date(),
  });
}, [userData]);
```

---

## 📝 Firestore Security Rules

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // อ่านได้ถ้าเป็นเจ้าของหรือ Admin
      allow read: if request.auth != null && 
        (request.auth.uid == userId || 
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin');
      
      // เขียนได้เฉพาะ Admin
      allow write: if request.auth != null && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
      
      // ป้องกันการแก้ไข suspended field โดยไม่ใช่ Admin
      allow update: if request.auth != null && 
        (request.auth.uid == userId || 
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin') &&
        (!request.resource.data.diff(resource.data).affectedKeys().hasAny(['suspended', 'suspendedUntil', 'suspendReason']) ||
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin');
    }
  }
}
```

---

## 🔄 Flow Diagram

```
┌─────────────────┐
│  Admin พักบัญชี  │
└────────┬────────┘
         │
         ├──► เลือกประเภท (ชั่วคราว/ถาวร)
         │
         ├──► กำหนดวันที่ (ถ้าชั่วคราว)
         │
         ├──► กรอกเหตุผล
         │
         ├──► บันทึกใน Firestore
         │
         └──► บันทึก Activity Log

┌──────────────────┐
│ User พยายาม Login │
└─────────┬────────┘
          │
          ├──► ตรวจสอบ suspended = true?
          │
          ├──► ตรวจสอบ suspendedUntil
          │
          ├──► ถ้าหมดอายุ → Auto-unsuspend
          │
          ├──► ถ้ายังไม่หมดอายุ → Redirect /suspended
          │
          └──► ถ้าไม่ถูกพัก → เข้าระบบปกติ
```

---

## 📚 Related Files

### Core Files
- `src/lib/userManagementUtils.ts` - ฟังก์ชันจัดการการพัก/ปลดพัก
- `src/contexts/AuthContext.tsx` - UserData interface และ context
- `src/components/ProtectedRoute.tsx` - Route protection และ auto-unsuspend
- `src/components/SuspendUserDialog.tsx` - UI dialog สำหรับพัก/ปลดพัก
- `src/pages/Suspended.tsx` - หน้าแสดงเมื่อถูกพัก
- `src/pages/Users.tsx` - หน้าจัดการผู้ใช้

### Supporting Files
- `src/lib/adminUtils.ts` - ฟังก์ชันโหลดข้อมูลผู้ใช้
- `src/lib/activityUtils.ts` - บันทึก Activity Log
- `src/App.tsx` - Route configuration

---

## 💡 Best Practices

### 1. เหตุผลในการพัก
- ควรระบุเหตุผลที่ชัดเจนและเป็นมืออาชีพ
- หลีกเลี่ยงข้อความที่ก้าวร้าวหรือไม่เหมาะสม
- เก็บเป็นหลักฐานในกรณีมีข้อพิพาท

### 2. ระยะเวลาการพัก
- พักชั่วคราวสำหรับการละเมิดครั้งแรก (7-30 วัน)
- พักถาวรสำหรับการละเมิดร้ายแรงหรือซ้ำซาก
- พิจารณาให้โอกาสผู้ใช้ก่อนพักถาวร

### 3. การสื่อสาร
- แจ้งผู้ใช้ก่อนพัก (ถ้าเป็นไปได้)
- แสดงข้อมูลการพักอย่างชัดเจน
- ระบุช่องทางติดต่อถ้ามีข้อสงสัย

### 4. การบันทึก
- บันทึก Activity Log ทุกครั้ง
- เก็บประวัติการพัก/ปลดพัก
- ใช้เป็นข้อมูลสำหรับตัดสินใจในอนาคต

---

## 🎯 สรุป

ระบบพักบัญชีผู้ใช้ช่วยให้ Admin สามารถจัดการพฤติกรรมที่ไม่เหมาะสมได้อย่างมีประสิทธิภาพ:

✅ **พักชั่วคราวหรือถาวร** - ยืดหยุ่นตามความรุนแรง  
✅ **ปลดพักอัตโนมัติ** - ลดภาระงาน Admin  
✅ **บันทึกเหตุผล** - เก็บหลักฐานและโปร่งใส  
✅ **Activity Logging** - ตรวจสอบย้อนหลังได้  
✅ **UI ที่ใช้งานง่าย** - จัดการได้ในไม่กี่คลิก  

---

## 📞 ติดต่อ

หากมีคำถามหรือพบปัญหา กรุณาติดต่อทีมพัฒนา

---

**Version:** 1.0  
**Last Updated:** October 2024  
**Author:** Game Nexus Dashboard Team



