# 👨‍💼 วิธีสร้าง Admin User แรก

## 🎯 ทำไมต้องสร้าง Admin User?

เมื่อติดตั้งระบบใหม่ จะยังไม่มี admin user ในระบบ คุณต้องสร้าง admin user แรกเพื่อ:
- เข้าถึงหน้าจัดการผู้ใช้
- อนุมัติ seller อื่นๆ
- จัดการระบบทั้งหมด

---

## 📋 วิธีที่ 1: สมัครแล้วแก้ใน Firestore Console (แนะนำ)

### ขั้นตอน:

#### 1. สมัครสมาชิกปกติ

```
1. ไปที่ http://localhost:8080/register
2. กรอกข้อมูล:
   - ชื่อร้าน: Admin Shop
   - อีเมล: admin@yourdomain.com
   - รหัสผ่าน: your-secure-password
3. คลิก "สมัครสมาชิก"
4. ยืนยันอีเมล (คลิกลิงก์ในอีเมล)
```

#### 2. เข้า Firestore Console

```
1. ไปที่ https://console.firebase.google.com/
2. เลือกโปรเจกต์ "game-shop-72ad1"
3. เมนูด้านซ้าย → Firestore Database
4. คลิก "Start collection" (ถ้ายังไม่มี) หรือ เปิด collection "users"
```

#### 3. แก้ไขข้อมูล User

```
1. หา document ที่มีอีเมลของคุณ
2. คลิกที่ document นั้น
3. แก้ไข 2 fields:
   
   role: "seller" → "admin"
   verified: true (ไม่ต้องเปลี่ยน เพราะ auto-verified แล้ว)
   
4. คลิก "Update" หรือ "Save"
```

#### 4. Logout และ Login ใหม่

```
1. กลับไปที่เว็บ http://localhost:8080
2. คลิก "ออกจากระบบ"
3. เข้าสู่ระบบอีกครั้งด้วย admin@yourdomain.com
4. ✅ คุณจะเห็น badge "ADMIN" ที่ sidebar!
5. ✅ คุณสามารถเข้าหน้า "จัดการผู้ใช้" ได้แล้ว!
```

---

## 📋 วิธีที่ 2: สร้างใน Firebase Console โดยตรง

### ขั้นตอน:

#### 1. สร้าง User ใน Authentication

```
1. Firebase Console → Authentication → Users
2. คลิก "Add user"
3. กรอก:
   - Email: admin@yourdomain.com
   - Password: your-secure-password
4. คลิก "Add user"
5. **คัดลอก User UID** (จะใช้ในขั้นต่อไป)
   ตัวอย่าง: "abc123def456ghi789"
```

#### 2. สร้าง Document ใน Firestore

```
1. Firestore Database → เปิด collection "users"
   (ถ้ายังไม่มี collection "users" ให้คลิก "Start collection")
2. คลิก "Add document"
3. กรอก Document ID: [paste UID จากขั้นที่ 1]
4. เพิ่ม fields:
```

| Field | Type | Value |
|-------|------|-------|
| uid | string | `abc123def456ghi789` (UID จาก Authentication) |
| email | string | `admin@yourdomain.com` |
| displayName | string | `Admin` |
| shopName | string | `Admin Shop` |
| role | string | `admin` |
| verified | boolean | `true` |
| emailVerified | boolean | `true` |
| createdAt | timestamp | `now` หรือวันที่ปัจจุบัน |

```
5. คลิก "Save"
```

#### 3. ทดสอบ Login

```
1. ไปที่ http://localhost:8080/login
2. Login ด้วย admin@yourdomain.com
3. ✅ เข้าสู่ระบบได้!
```

---

## 📋 วิธีที่ 3: ใช้ Script (Advanced)

สร้างไฟล์ `scripts/create-admin.js`:

```javascript
const admin = require('firebase-admin');

// Initialize Firebase Admin SDK
const serviceAccount = require('./serviceAccountKey.json');

admin.initializeApp({
  credential: admin.credential.cert(serviceAccount)
});

const auth = admin.auth();
const db = admin.firestore();

async function createAdminUser() {
  const email = 'admin@yourdomain.com';
  const password = 'your-secure-password';
  const displayName = 'Admin';
  
  try {
    // สร้าง user ใน Authentication
    const userRecord = await auth.createUser({
      email: email,
      password: password,
      displayName: displayName,
      emailVerified: true,
    });

    console.log('✅ Created user:', userRecord.uid);

    // สร้าง document ใน Firestore
    await db.collection('users').doc(userRecord.uid).set({
      uid: userRecord.uid,
      email: email,
      displayName: displayName,
      shopName: 'Admin Shop',
      role: 'admin',
      verified: true,
      emailVerified: true,
      createdAt: admin.firestore.FieldValue.serverTimestamp(),
    });

    console.log('✅ Created Firestore document');
    console.log('✅ Admin user created successfully!');
    console.log('   Email:', email);
    console.log('   Password:', password);
    
  } catch (error) {
    console.error('❌ Error:', error);
  }
  
  process.exit();
}

createAdminUser();
```

**รัน:**
```bash
node scripts/create-admin.js
```

---

## 🔍 ตรวจสอบว่าสำเร็จหรือไม่

### เช็คใน Authentication
```
1. Firebase Console → Authentication → Users
2. หาอีเมล admin@yourdomain.com
3. ✅ ควรเห็น user นี้
```

### เช็คใน Firestore
```
1. Firebase Console → Firestore → users
2. หา document ที่ email = admin@yourdomain.com
3. ✅ ตรวจสอบ fields:
   - role: "admin"
   - verified: true
   - emailVerified: true
```

### เช็คในเว็บ
```
1. Login ที่ http://localhost:8080/login
2. ✅ ดูที่ sidebar ควรเห็น:
   - Badge "ADMIN" ข้างชื่อ
   - Shield icon ที่รูปโปรไฟล์
3. ✅ เข้าหน้า "จัดการผู้ใช้" ได้
```

---

## 🎯 หลังจากมี Admin แล้ว

### สร้างผู้ใช้เพิ่ม

```
1. เข้าหน้า "จัดการผู้ใช้" (/users)
2. คลิก "เพิ่มผู้ใช้"
3. กรอกข้อมูล:
   - อีเมล
   - ชื่อร้าน
   - ชื่อแสดง
   - รหัสผ่าน
   - เลือก Role (admin/seller)
   - เปิด "อนุมัติการใช้งานทันที" (ถ้าต้องการ)
4. คลิก "สร้างผู้ใช้"
5. ✅ ระบบจะส่งอีเมลยืนยันให้ผู้ใช้อัตโนมัติ
```

### อนุมัติ Seller ที่สมัครเอง

```
1. Seller สมัครที่ /register
2. Seller ยืนยันอีเมล
3. Seller login → เจอหน้า "รอการอนุมัติ"
4. Admin เข้าหน้า "จัดการผู้ใช้"
5. หา Seller → คลิก "แก้ไข"
6. เปิด "อนุมัติการใช้งาน"
7. บันทึก
8. ✅ Seller สามารถ login ได้แล้ว!
```

---

## ⚠️ ข้อควรระวัง

### อย่าลบ Admin User เดียวในระบบ!

ถ้าคุณลบ admin user เดียว:
- ❌ จะไม่มีใครเข้าหน้าจัดการผู้ใช้ได้
- ❌ ไม่สามารถอนุมัติ seller ใหม่ได้
- 🔧 ต้องสร้าง admin ใหม่ผ่าน Firestore Console

**แนะนำ:**
- สร้าง admin อย่างน้อย 2 คน
- ใช้อีเมลที่ต่างกัน
- เก็บรหัสผ่านไว้อย่างปลอดภัย

---

## 🐛 Troubleshooting

### ปัญหา: Login แล้วไม่เห็น badge "ADMIN"

**แก้ไข:**
1. Logout แล้ว Login ใหม่
2. Clear browser cache
3. ตรวจสอบ Firestore ว่า `role` เป็น `"admin"` หรือไม่
4. ตรวจสอบ Console (F12) มีข้อผิดพลาดหรือไม่

### ปัญหา: ไม่สามารถเข้าหน้า "จัดการผู้ใช้" ได้

**แก้ไข:**
1. ตรวจสอบ Firestore:
   - `role` ต้องเป็น `"admin"` (string)
   - ไม่ใช่ `"Admin"` หรือ `"ADMIN"`
2. Logout และ Login ใหม่
3. ตรวจสอบ console มีข้อผิดพลาดหรือไม่

### ปัญหา: ส่งอีเมลยืนยันไม่ได้

**แก้ไข:**
1. ตรวจสอบ Firebase Console → Authentication → Templates
2. ตรวจสอบ Email provider settings
3. ลองใช้อีเมลจริง (ไม่ใช่ test email)

---

## ✅ Checklist

ก่อน Deploy ให้ตรวจสอบ:

- [ ] มี Admin user อย่างน้อย 1 คน
- [ ] Admin user มี `role: "admin"`
- [ ] Admin user มี `verified: true`
- [ ] Admin user มี `emailVerified: true`
- [ ] สามารถ login ด้วย admin ได้
- [ ] เห็น badge "ADMIN" ใน sidebar
- [ ] เข้าหน้า "จัดการผู้ใช้" ได้
- [ ] สร้างผู้ใช้ใหม่จาก admin panel ได้
- [ ] ระบบส่งอีเมลยืนยันได้

---

## 🎉 เสร็จสิ้น!

ตอนนี้คุณมี Admin user แล้ว พร้อมที่จะ:
- ✅ จัดการผู้ใช้ทั้งหมด
- ✅ สร้างผู้ใช้ใหม่
- ✅ อนุมัติ seller
- ✅ เปลี่ยน role
- ✅ ใช้งานระบบเต็มรูปแบบ

**Happy Managing! 🚀**



